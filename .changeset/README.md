# Changesets

This folder is managed by [`@changesets/cli`](https://changesets.dev). Add a changeset when a published `@homeslate/*` package should be released:

```bash
pnpm changeset
```

Merging to `main` opens a **Version Packages** PR. Merging that PR publishes to npm.
